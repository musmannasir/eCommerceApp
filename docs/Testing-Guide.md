# Testing Guide

## Test projects

| Project | Purpose |
|---|---|
| `tests/ECommerceApp.Domain.Tests` | Pure unit tests for domain entities, value objects, and `Result`/`Error`. No I/O. Includes `OrderStatusTransitionsTests` (Milestone 10.3) - every legal transition in the state graph, every illegal one explicitly rejected, and every terminal status (`PaymentFailed`/`StockReservationFailed`/`Cancelled`/`Delivered`) allowing no further transition to any status at all. |
| `tests/ECommerceApp.Application.Tests` | Unit tests for FluentValidation validators (`RegisterRequestValidator`, `ChangePasswordRequestValidator`, `PromotionValidatorsTests` - valid entire-order request, empty name, invalid discount/scope type, over-100% percentage, zero discount value, a category scope missing its category id, end-before-start dates, etc.; `TaxRateValidatorsTests` - valid country-wide and region-scoped requests, empty/malformed country code, empty tax category, negative/over-100 rate, a zero rate being valid; `ShippingMethodValidatorsTests` - valid country-wide and region-scoped requests, empty name, malformed country code, negative base rate/rate per kg/free-shipping threshold, a max delivery days below the minimum, a negative display order; `AddressValidatorsTests` - Milestone 8.1 - a well-formed request, one with every optional field omitted, each required field rejected when empty, a malformed country code, and the update validator rejecting a zero id). |
| `tests/ECommerceApp.Infrastructure.Tests` | EF Core behavior tests (InMemory provider), `AuthService` tests against a real Identity stack (`UserManager`/`SignInManager`/`RoleManager`) backed by InMemory - see `AuthServiceTestHarness` - catalog service tests (`CatalogTestHarness`, shared with `HomePageBannerServiceTests`/`HomePageServiceTests`/`CatalogBrowseServiceTests`/`CatalogBrowseFilterTests`) covering Category/Brand/Product/variant creation, duplicate slugs/SKUs/combinations, circular category parents, soft delete, pagination, publication rules, home-page-banner CRUD, home-page section composition (unpublished/inactive product exclusion, featured/new-arrival/discounted filtering, imageless-banner exclusion), catalog browsing (pagination, unpublished/inactive exclusion, out-of-stock baseline non-exclusion, category-descendant inclusion, brand/search filtering, unknown-slug failures), and catalog filtering/sorting/suggestions (search matching across brand/category, search-input safety with special characters, every filter individually and combined, every sort option including relevance ranking, suggestions matching and empty-term handling), product detail composition (`ProductDetailServiceTests` - published/active/soft-delete requirements, direct-variant-id and attribute-combination resolution, invalid-combination fallback, stock aggregation across in-stock/low-stock/out-of-stock/backorder/untracked states, related-products scoping, variant-combination matrix contents, and the strict `ResolveVariantAsync` path rejecting a variant that's inactive, belongs to a different product, doesn't exist, or whose product is unpublished), pricing calculation (`PricingServiceTests` - base vs. variant price/compare-at overrides, discount amount/percent, the always-zero promotion adjustment, and the store-settings-driven tax-inclusive flag, now via a mocked `IStoreSettingsService` rather than raw `IConfiguration`), recommendation scoring (`RecommendationServiceTests` - category/brand/tag/price-range signals individually and combined, score-based ordering, unpublished/inactive/self exclusion, empty-result handling, and the requested-count cap), cart core and merge/pricing integrity (`CartServiceTests` - add/merge into an existing line, variant-required/not-applicable/invalid rejections, stock validation including the untracked/backorder leniency and combined-quantity-across-calls checks, update/remove/clear, ownership isolation between two different owners, a soft-deleted product's line still displaying via `IgnoreQueryFilters()` while being excluded from totals, guest-to-user cart merge both with and without an existing user cart including the combined-quantity stock cap, a price change since adding being flagged without affecting LineTotal, an explicit quantity update re-stamping the price and clearing the flag, and a stock shortfall being flagged without silently changing the stored Quantity), promotions and coupon application (`PromotionServiceTests` - CRUD, coupon-code conflict rejection, case-insensitive code lookup, a valid percentage/fixed-amount code computing the right discount, `MaxDiscountAmount` capping a percentage discount, a fixed-amount discount never exceeding the eligible amount, expired/not-yet-started/inactive/minimum-order-not-met rejection, a category-scoped code only discounting matching lines and being rejected outright when nothing matches, and an unknown code being not-found; plus `CartServiceTests` additions for `ApplyCouponAsync`/`RemoveCouponAsync` - a valid coupon setting the discount/total, an unknown code failing without changing the cart, applying to an empty cart being rejected, removing clearing the discount, and a promotion deactivated after being applied getting silently cleared on the next cart read; plus Milestone 7.4 additions for `LineDiscounts` - an entire-order promotion splitting the discount proportionally across all lines, a category-scoped promotion zeroing the discount for lines outside its scope, and the allocated line discounts always summing to exactly the total discount amount despite rounding), tax rates and estimation (`TaxServiceTests` - CRUD, duplicate country-wide/region-scoped rate rejection, a country-wide and a region-scoped rate for the same country/category coexisting, an exact region match taking precedence over a country-wide fallback, no match at all returning zero tax with `RateConfigured` false, case-insensitive country/category matching, an inactive rate not applying, and `CalculateEstimatedTaxAsync` summing across multiple categories, returning unconfigured for no lines, and returning unconfigured when nothing matches the store's default jurisdiction; plus `CartServiceTests` additions for `EstimatedTax` - a taxable item showing the estimate when a rate is configured, a non-taxable item being excluded, no rate configured yielding zero/unconfigured, and lines with different tax categories summing correctly), shipping methods and estimation (`ShippingServiceTests` - CRUD, duplicate-name-per-jurisdiction rejection, the same name allowed in a different country, cost as base rate plus rate-per-kg times weight, the free-shipping threshold zeroing the cost when met and not affecting it when unmet, multiple methods for the same jurisdiction all being returned (unlike Tax's single-rate precedence), a whole-country and a region-specific method both applying to a matching region, a region-specific method not applying to a different region, no methods configured returning an empty list, an inactive method being excluded, and `CalculateEstimatedShippingAsync` picking the cheapest available option or returning unconfigured when nothing matches; plus `CartServiceTests` additions for `EstimatedShipping` - a configured method producing the estimate, a product with no recorded weight contributing 0kg, no method configured yielding zero/unconfigured, and weight summing correctly across multiple lines), the checkout calculation service (`CheckoutCalculationServiceTests` - Milestone 7.4 - no promotion/tax/shipping configured leaving the grand total equal to the subtotal, an entire-order discount reducing the taxable amount before tax is calculated, a category-scoped discount only reducing the tax owed on matching lines, the shipping free-threshold being checked against the post-discount subtotal, an invalid applied promotion id being treated as no discount rather than failing, and a non-taxable line being excluded from tax even when a rate is configured; plus `CartServiceTests` additions confirming applying a coupon reduces the amount `EstimatedTax`/`EstimatedShipping` are computed against - including the free-shipping-threshold crossing case - and that `EstimatedGrandTotal` correctly combines `Total` with both estimates, or equals `Total` when nothing is configured; plus Milestone 8.2 additions for `GetCheckoutInputAsync` - no cart/an empty cart/only-unavailable-lines all rejected as `"cart.empty"`, a checkout line built per available item with the correct line total and total weight, and the resolved applied promotion id being returned), wishlist (`WishlistServiceTests` - toggle add/remove idempotency, an unpublished/nonexistent product rejected as not-found, most-recently-added-first ordering, silent exclusion of a product that became unpublished since being saved, `IsWishlistedAsync` reflecting current state, idempotent removal, and ownership isolation between two different users), the address book (`AddressServiceTests` - Milestone 8.1 - a customer's first address always becoming the default even when not requested, a second address created as default clearing the first one's flag (and leaving it untouched when not requested), getting/updating/deleting an address that exists but belongs to a different user returning not-found the same as one that doesn't exist at all, updating an address to be the default clearing the previous one, deleting the default address leaving no default at all, `SetDefaultAsync` clearing the previous default, and two different users' addresses being fully isolated), orders (`OrderServiceTests` - Milestone 9.1 - creating an order succeeds and persists every snapshotted field (address, shipping, totals, items), resubmitting the same idempotency key returns the original order rather than creating a duplicate or applying the second submission's (different) totals, two different users can independently use the identical idempotency key without colliding, an unknown idempotency key or order number returns not-found, and `GetByOrderNumberAsync` is ownership-scoped exactly like `AddressServiceTests` - a different user's order number returns not-found rather than their data; plus Milestone 9.2 additions - creating an order with the Stripe test success card marks it Paid with the masked card/brand persisted, and the Stripe test decline card marks it PaymentFailed with the decline reason persisted while still producing a real, persisted order; plus Milestone 9.3 additions - a paid order reserves stock for a tracked product, a payment failure releases the just-created reservation, insufficient stock fails reservation with `StockIssueMessage` set and without ever charging the card, an untracked product (no `InventoryItem` row at all) skips reservation entirely and still charges normally, and a multi-line order where a later line fails releases the earlier line's already-successful reservation; plus Milestone 10.1 additions for `GetPagedAsync` - orders returned newest-first, search matching by order number or customer name, filtering by status, and correct pagination across multiple pages; plus Milestone 10.2 additions - `GetByIdAsync` returning an order regardless of which user placed it and a not-found for an unknown id, `CancelAsync` releasing a paid order's reservation and marking it Cancelled, `CancelAsync` rejecting a non-paid order, and `UpdateAdminNotesAsync` saving and returning the note; plus Milestone 10.3 additions - `ShipAsync` consuming the reservation and marking a paid order Shipped (with the on-hand quantity actually decreasing), `ShipAsync` rejecting a non-paid order, `MarkDeliveredAsync` marking a shipped order Delivered and rejecting one that hasn't shipped, and `CancelAsync` now rejecting an order that has already shipped; plus Milestone 11.1 additions for `GetDashboardAsync` - only including the given user's own orders, `TotalSpent` counting only successfully-charged orders while `TotalOrders` still counts a declined one, and correct pagination; plus Milestone 13.1 additions for `CancelOwnOrderAsync` - releasing the reservation and marking a paid order cancelled, rejecting a non-paid order, and returning not-found for another user's order number), Milestone 11.3 additions for `ReorderAsync` (`CartServiceTests`) - every line adding successfully when all products are still available, a deactivated product being skipped while the remaining lines still succeed, and every line being skipped (added count zero) when every product is unavailable, reviews and rating summary (`ReviewServiceTests` - Milestone 12.1) - submitting a review succeeds and is unverified without a purchase, a second review for the same product from the same user is rejected as a conflict, submitting for an unpublished product is not-found, a review is verified when the reviewer has a genuinely-charged order containing the product, `HasReviewedAsync` reflecting submission state, `GetRatingSummaryAsync` computing the average and a zero-filled 1-5 star breakdown, and `GetReviewsAsync` returning newest-first with correct pagination; plus Milestone 12.2 additions - reporting a review succeeds, reporting the same review twice by the same user is rejected as a conflict, reporting an unknown review is not-found, the moderation queue only includes reviews with open reports (with the correct report count/detail), dismissing reports clears them while the review stays live and queryable, removing a review soft-deletes it and excludes it from every read (rating summary, review list, and the moderation queue itself), and dismissing/removing an unknown review is not-found, returns and their approve/reject workflow (`ReturnServiceTests` - Milestone 13.2) - submitting a return request succeeds for a Delivered order, is rejected as validation for a non-Delivered order, is not-found for another user's order, is rejected as validation for a quantity exceeding what was ordered or for an empty item list, is rejected as a conflict when an open (Requested/Approved) request already exists for the order, and succeeds again after a prior request was rejected; `ApproveAsync`/`RejectAsync` succeeding for a pending request and rejecting as validation when the request isn't pending; `GetPendingQueueAsync` only returning Requested requests with correct pagination; and `GetReturnRequestsForOrderAsync` returning every request for an order newest-first; plus Milestone 13.3 additions - `RefundAsync` refunding the returned items' line total and restocking the exact original warehouse for an Approved request, transitioning it to Refunded, rejecting a request that isn't Approved, and returning not-found for an unknown request, and `GetAwaitingReceiptQueueAsync` only returning Approved requests), restocking a returned item (`InventoryServiceTests` - Milestone 13.3) - `RestockReturnedItemAsync` increasing on-hand quantity and recording a CustomerReturn movement referencing the return request, and rejecting an unknown inventory item, the financial dashboard and ledger (`FinanceServiceTests` - Milestone 14.1) - `GetDashboardSummaryAsync` returning all zeros when nothing has happened yet, correctly computing TotalRevenue/TotalRefunded/NetRevenue/PaidOrderCount/RefundCount/AverageOrderValue across a mix of paid, declined, and refunded orders (a declined order's Payment row never counts, since its Status isn't Succeeded), and `GetLedgerAsync` merging charges and refunds newest-first with signed amounts (a refund negative, a charge positive) and paginating correctly; plus Milestone 14.2 additions for `GetCashFlowAsync` - defaulting to the 30 days ending today when no range is given, correctly bucketing charges and refunds by calendar day while filling gap days with zero rather than omitting them, and silently swapping a reversed From/To range rather than rejecting it, the Top Selling Products report and CSV export helper (`ReportingServiceTests`/`CsvExportTests` - Milestone 14.3) - `GetTopSellingProductsAsync` grouping and summing correctly by product and ordering by quantity descending, excluding orders that were never successfully charged, respecting the `Take` limit, defaulting to the 30 days ending today, and ignoring orders outside an explicit requested range; and `CsvExport.BuildCsv` writing a header row followed by each data row, quoting a field containing a comma/quote/newline (with an embedded quote correctly doubled), and leaving a plain field unquoted, the simulated payment gateway (`SimulatedPaymentGatewayTests` - Milestone 9.2 - the Stripe success/decline test-card numbers producing the expected outcome, a card number failing the Luhn checksum or with an invalid length declining as invalid, an invalid CVV length/format declining, an expired card declining while one expiring in the current month still succeeds, the card brand being detected correctly from Visa/Mastercard/American Express/Discover leading digits, and the masked number never exposing more than the last four digits), and inventory service tests (`InventoryTestHarness`, shared with `SupplierServiceTests`/`PurchaseOrderServiceTests`) covering opening stock, adjustments, reservation create/release/consume (Milestone 10.3's `ConsumeReservationAsync` deducting on-hand quantity and recording a `SaleCompletion` movement, and rejecting an already-released reservation), overselling/backorder rules, low/out-of-stock status detection, supplier CRUD/soft-delete, supplier-product linking, and the full purchase-order lifecycle (draft→submit→approve→receive, full/partial receipt, over-receipt rejection with/without an audited override, cancellation-window rules); and email notifications (`EmailNotificationServiceTests` - Milestone 15.1, using hand-rolled `FakeEmailTemplateRenderer`/`FakeEmailSender` stand-ins the same way `FakeFileStorage`/`FakeClock` already do elsewhere in this project rather than a mocking library) - `SendPasswordResetEmailAsync` rendering the `Emails/PasswordReset` template with the given reset link and sending the rendered HTML to the given address with the subject "Reset your password", and `SendOrderConfirmationEmailAsync` rendering the `Emails/OrderConfirmation` template with the given order model and sending it with a subject naming the order number; the transactional outbox (`OutboxProcessorTests` - Milestone 15.2, using a `FakeEmailNotificationService` stand-in the same way `EmailNotificationServiceTests` fakes one layer lower) - a pending password-reset or order-confirmation message is sent through `IEmailNotificationService` and marked Processed with `ProcessedAtUtc` set, a message whose send throws increments `Attempts` and records `LastError` while staying Pending, a message that keeps failing is marked terminally Failed once it reaches the 5-attempt cap, a failure on one message never prevents another pending message in the same batch from being processed, and an already-Processed or -Failed message is left untouched by a later run; plus `OrderServiceTests` additions - a paid order enqueues an `OrderConfirmationEmail` outbox row atomically with the order itself (correct recipient/order number/total in the payload), and a declined order enqueues nothing; plus `PasswordManagementTests` additions - `ForgotPasswordAsync` enqueues a `PasswordResetEmail` outbox row (readable back out via the test's identity `buildResetLink` callback) for a known active account, and enqueues nothing for an unknown email; the outbox background job (`OutboxProcessingBackgroundServiceTests` - Milestone 15.3, using a `FakeOutboxProcessor` and real elapsed time rather than `FakeClock` - the only place in this suite that does, since the class under test is itself the thing deciding when to wake up) - a pending sweep runs immediately on `StartAsync` without waiting for the first interval, a second sweep runs again once the configured interval elapses, and a sweep that throws is caught internally and does not stop the loop from ticking again on the next interval; admin user management (`UserManagementServiceTests` - Milestone 16.1, against the same real Identity stack `AuthServiceTestHarness` already provides) - `GetPagedAsync` returns every user with their resolved role, search matches name or email, filtering by role and by active status both work; `CreateAsync` creates a user with the given role, rejects a duplicate email, rejects an unknown role, and writes a `UserCreatedByAdmin` audit event; `UpdateAsync` changes the role and writes a `UserRoleChanged` audit event, rejects an admin changing their own role, but still allows an admin to edit their own name without changing role; `DeactivateAsync`/`ActivateAsync` round-trip `IsActive` and `DeactivateAsync` rejects an admin deactivating their own account; `UnlockAsync` clears a real Identity lockout and writes a `UserUnlocked` audit event; and `GetByIdAsync` returns not-found for an unknown id; the audit log viewer (`AuditLogServiceTests` - Milestone 16.2) - `GetPagedAsync` returns events with the user's email resolved, leaves `UserEmail` null for an event with no user rather than excluding it, filters by event type and by succeeded/failed, search matches the resolved user email, events outside the (default 30-day) date range are excluded, the default range is exactly the 30 days ending today, results are returned newest-first, and `GetAllAsync` returns every matching row unpaginated (25 seeded rows) while `GetPagedAsync` correctly caps the same query at one page (20); the store settings service (`StoreSettingsServiceTests` - Milestone 16.3) - `GetAsync` falls back to appsettings.json's old default values when no row exists, returns the seeded row's real values when one does, and a second call doesn't re-query the database after the row was deleted out from under it (proving the `IMemoryCache` layer is genuinely in effect, not just present); `UpdateAsync` persists every field, writes a `StoreSettingsUpdated` audit event, and invalidates the cache so an immediately-following `GetAsync` reflects the change - the stale-`RowVersion` conflict path itself is deliberately not exercised here, since the EF Core InMemory provider's concurrency-token behavior doesn't reliably reproduce SQL Server's; that path is covered instead by `SettingsFlowTests` against the real test database. Plus `LocalFileStorageTests` (real disk I/O) and `ImageSignatureDetectorTests` (magic-byte detection). |
| `tests/ECommerceApp.Web.Tests` | Unit tests for controllers, services, and middleware in isolation (no real HTTP host). Includes `RecentlyViewedServiceTests` - a real `ApplicationDbContext` (InMemory) plus a `DefaultHttpContext`/`HttpContextAccessor`, since the service under test lives here (not Infrastructure) for its `HttpContext` dependency: guest cookie round-trips across simulated separate requests (recording, dedup-and-move-to-front, trim-to-max, the max now driven by a mocked `IStoreSettingsService` rather than raw `IConfiguration` since Milestone 16.3), authenticated DB upsert/trim, current-product exclusion, and graceful skip of a product that became unpublished after being viewed. Also `CartOwnerAccessorTests` - authenticated-user resolution, a guest with no cookie getting a new one (and a response cookie actually being set), an existing cookie being reused without setting a new one, `TryGetOwner()` never planting a cookie for a read-only request, and (Milestone 6.2) `TryGetGuestToken()` reading the cookie directly regardless of auth state and `ClearGuestToken()` issuing a delete. |
| `tests/ECommerceApp.IntegrationTests` | Architecture/dependency-rule tests, full-pipeline smoke tests, and full-stack tests against a real SQL Server test database via `WebApplicationFactory<Program>` - admin-area authorization (auth, catalog, inventory, suppliers, purchase orders, and home page banners), storefront authorization (a customer browsing the public site cannot reach catalog admin), the four public catalog listing routes end-to-end including every sort option, a fully-combined filter request, and the JSON suggestions endpoint (`CatalogBrowseFlowTests` - specifically proves the product-card EF projection and filter/sort query shapes translate against real SQL Server, not just the InMemory provider), the product detail page end-to-end including a variant and its related products, plus the live-resolution endpoint's success and rejection paths (`ProductDetailFlowTests`), Cart's AJAX endpoints end-to-end including the guest-cookie round trip across separate requests on the same `HttpClient`, a stock-limit rejection with a descriptive message, a request missing the CSRF header being rejected, and two separate clients' guest carts being isolated (`CartFlowTests` - also proves `CartService`'s `IgnoreQueryFilters()`/dictionary-projection queries translate against real SQL Server), the guest-to-user cart merge flow driven through the real `AccountController` login/register actions on one cookie-persisting `HttpClient` - both the "user has no cart yet" fast path (register while carrying a guest cart) and the "both exist" line-by-line path (log back in with a second guest cart after logging out) (`CartMergeFlowTests`), Wishlist's `[Authorize]` gate actually rejecting an anonymous AJAX-style request with a real `401` (not a silently-followed redirect - the exact bug this milestone's tests caught), an anonymous page load still redirecting to login normally, and a signed-in customer toggling a product on and off their wishlist end-to-end including the wishlist page rendering it (`WishlistFlowTests`), admin-area authorization for promotions the same shape as home page banners (`PromotionAuthorizationTests`), applying a valid coupon over real HTTP updating the cart's discount/total, an unknown code returning a `404`, and removing a coupon clearing the discount (`CouponFlowTests` - also proves `PromotionService`'s case-insensitive code lookup translates against real SQL Server), admin-area authorization for tax rates the same shape as promotions (`TaxRateAuthorizationTests`), adding a taxable item returning an estimated tax over real HTTP when a rate is configured for the app's default jurisdiction vs. zero/unconfigured when it isn't (`EstimatedTaxFlowTests`), admin-area authorization for shipping methods the same shape (`ShippingMethodAuthorizationTests`), and adding an item returning an estimated shipping cost over real HTTP when a method is configured for the app's default jurisdiction (`EstimatedShippingFlowTests` - deliberately has no "unconfigured" counterpart, since Shipping's `(CountryCode, RegionCode)` lookup key, unlike Tax's category-inclusive one, gives no per-test-randomizable dimension to safely assert "nothing configured" against the app's one fixed default jurisdiction in a database that's only reset once per collection - that behavior is covered instead by `ShippingServiceTests`/`CartServiceTests` against a fresh InMemory database per test), the address book's classic MVC forms end-to-end - an anonymous visitor loading `/Addresses` being redirected to login, a signed-in customer creating an address via a real form POST and seeing it listed as the default, and a second customer's `Edit`/`Delete` requests against the first customer's address id both failing (a `404` on Edit, no actual removal on Delete) rather than succeeding across accounts (`AddressFlowTests` - Milestone 8.1), the Checkout flow's classic MVC forms end-to-end - an anonymous visitor loading `/Checkout` redirected to login, a signed-in customer with an empty cart redirected to the Cart page, a customer with items but no saved addresses redirected to add one, the full address -> shipping -> review flow computing the correct real, destination-based subtotal/tax/shipping/total (distinct from the Cart page's store-default-jurisdiction estimate), and a customer unable to use another customer's address id anywhere in the flow (`CheckoutFlowTests` - Milestone 8.2; plus Milestone 8.3 additions - a cart whose stock has since dropped below the reviewed quantity being redirected to the Cart page both at `GET /Checkout` and at final `PlaceOrder` submission rather than only being informationally flagged, a successful `PlaceOrder` showing the Confirmation page with the same reviewed totals, resubmitting the exact same idempotency key after stock has since been depleted still replaying the original successful confirmation instead of re-validating and failing, and a stale/tampered `shippingMethodId` on `PlaceOrder` redirecting back to the Shipping step the same way the existing Review-step guard does; plus Milestone 9.1 additions - a successful `PlaceOrder` landing on `/Checkout/Confirmation/{orderNumber}` (a real, persisted order number, not the old idempotency-key-keyed cache route) with the cart genuinely emptied afterward, resubmitting the same idempotency key resolving to the exact same order number rather than a second one, and the created `Order`/`OrderItem` rows queried directly against real SQL Server to confirm the snapshotted address/shipping/item fields match what Review showed; plus Milestone 9.2 additions - placing an order with the Stripe test success card marks it Paid and clears the cart, and placing an order with the Stripe test decline card marks it PaymentFailed, shows the decline reason on Confirmation, and leaves the cart untouched so the customer can immediately retry; plus Milestone 9.3 additions - placing an order reserves stock and the reservation is visible directly against real SQL Server afterward, and a product whose stock is split across two warehouses (each individually below the ordered quantity, but sufficient in aggregate - passing the Milestone 8.3 stock guard) correctly fails reservation at the single best-fit warehouse, leaves the order `StockReservationFailed` with a populated `StockIssueMessage`, creates zero `Payment` rows, leaves every warehouse's reservation at zero, and does not clear the cart), open-redirect protection, the JWT API surface, the MVC change-password flow, RowVersion concurrency conflicts (catalog, inventory, and purchase-order items), admin-area authorization for the order queue the same shape as purchase orders - anonymous redirected to login, a `Customer`/`CatalogManager` rejected with a real `403`, and both an `OrderManager` and a `CustomerSupport` user (the two roles the `CanManageOrders` policy grants) able to view it (`OrderAuthorizationTests` - Milestone 10.1), the admin order detail page end-to-end against a real customer order placed through the full storefront checkout flow - staff can view its full detail, save an internal note and see it persist, and cancel a paid order (confirming the real `Cancelled` status and the released stock reservation directly against SQL Server), plus a non-paid order correctly rejecting a cancel attempt (`OrderDetailFlowTests` - Milestone 10.2; plus Milestone 10.3 additions - shipping a paid order with a carrier and tracking number then marking it delivered end-to-end, confirming via direct SQL query that the reservation was actually consumed (on-hand quantity decreased, not just released) and the order reached the terminal Delivered status), the customer-facing "My Orders" dashboard end-to-end - anonymous redirected to login, a customer with no orders sees the empty state, and a customer who places both a declined and a successful order over the real storefront checkout flow sees both in their own list (TotalOrders counting both, TotalSpent counting only the successful charge) while a different customer's dashboard shows neither (`MyOrdersFlowTests` - Milestone 11.1; plus Milestone 11.2 additions - a customer views their own order's detail page and sees the tracking timeline, a second customer gets a `404` on both the Details and Invoice URLs for an order that isn't theirs, a paid order's invoice renders with the item/store details, and a declined order's invoice route redirects back with an explanatory message instead of rendering; plus Milestone 11.3 additions - a customer reordering a past order's items sees the redirect to `/Cart` with the correct "Added" summary and the item present, reordering an order containing a product deactivated since it was placed shows the "could not be added" summary instead, and a second customer attempting to reorder an order that isn't theirs gets a `404`; plus Milestone 13.1 additions - a customer cancelling their own paid order sees the "Order cancelled." message, the Cancelled status, and the Cancel button correctly gone afterward, a second customer cannot cancel another customer's order (`404`), and cancelling a non-paid order is rejected with an explanatory message), the product page's review submission and rating summary end-to-end (`ReviewFlowTests` - Milestone 12.1) - an anonymous visitor sees a login prompt instead of the review form, a signed-in customer submits a review and sees it immediately with the updated rating summary, a customer who actually purchased the product (driven through the real storefront checkout flow) gets the Verified Purchase badge, and submitting a second review for the same product is rejected with an explanatory message instead of duplicating (`ReviewFlowTests` - Milestone 12.1), the review moderation flow end-to-end (`ReviewModerationFlowTests` - Milestone 12.2) - admin-area authorization for the moderation queue the same shape as the order queue (anonymous redirected to login, a plain `Customer` rejected with a real `403`, a `CustomerSupport` user able to view it), reporting a review surfaces it in the moderation queue with the correct report count/detail and dismissing it clears the queue while the review stays visible on the product page, removing a reported review hides it from the product page entirely, and reporting the same review twice by the same reporter is rejected with an explanatory message, the return-request flow end-to-end (`ReturnFlowTests` - Milestone 13.2) - admin-area authorization for the return queue the same shape as the review moderation queue (anonymous redirected to login, a plain `Customer` rejected with a real `403`, a `CustomerSupport` user able to view it), a customer requesting a return on a real Delivered order (shipped and delivered via the admin UI) sees it show as "Requested" and the admin queue lists it, approving it from the admin queue empties the queue and the customer's Details page updates to "Approved", rejecting a request shows the customer the rejection reason and still allows a fresh resubmission afterward, a customer cannot request a return for another customer's order (`404`), and requesting a return for a non-Delivered order shows a validation error; plus Milestone 13.3 additions - marking an approved return received refunds the returned items' line total and restocks the exact original warehouse, moves the request from the admin "Awaiting receipt" section to gone entirely, and shows the customer a "Refunded" status with the amount, all confirmed via direct SQL query against the real Refunds/InventoryItems tables, the admin financial dashboard and ledger (`FinanceFlowTests` - Milestone 14.1) - admin-area authorization for the ledger the same shape as other admin queues but scoped to the newly-wired CanViewFinancialReports policy (anonymous redirected to login, OrderManager rejected with a real `403`, Admin able to view it), the dashboard hiding its financial cards from a role without CanViewFinancialReports while still loading successfully for that role, the dashboard showing financial cards to an Admin, a real order followed by an approve-and-refund flow showing up correctly on both the dashboard totals and the ledger feed (confirmed via direct SQL query against the real Refunds table), and CustomerSupport able to approve a return but rejected with a real `403` attempting to refund it - confirming the Milestone 13.3 authorization gap this milestone closed; plus Milestone 14.2 additions - admin-area authorization for cash flow the same CanViewFinancialReports shape as the ledger (OrderManager rejected with a real `403`), a real order showing up on today's row with the site-wide daily total confirmed independently via direct SQL query rather than a hardcoded figure (this shared, only-reset-once-per-collection database means other tests' same-day orders also contribute to the total), and an explicit date-range filter reflecting back the requested From/To dates, the admin reports hub and CSV export (`ReportsFlowTests` - Milestone 14.3) - admin-area authorization for the newly-enabled `/Admin/Reports` the same CanViewFinancialReports shape (OrderManager rejected with a real `403`), the reports hub linking to all three reports and the Top Selling Products report rendering, a real order showing up on the Top Selling Products report and its CSV export, and the Ledger and Cash Flow CSV export endpoints both returning a real `text/csv` response containing a real transaction, email notifications (`NotificationFlowTests` - Milestone 15.1, reading the real HTML file `DevEmailSender` writes to `Logs/DevEmails` rather than mocking `IEmailSender` - `WebApplicationFactory` hosts the app in-process, so `Directory.GetCurrentDirectory()` resolves identically for the test and the app under test) - requesting a password reset writes a rendered email containing the subject line and a working `/Account/ResetPassword` link for that address, placing and paying for a real order over the full storefront checkout flow writes a rendered order-confirmation email with the correct order number and grand total, and a declined payment does not write any confirmation email at all - unchanged by Milestone 15.2's transactional-outbox rework, now with added assertions that the enqueued `OutboxMessage` row itself reaches `Processed` status (or, for the declined-payment case, that no row mentioning that order's recipient was ever created), proving the outbox is a purely internal durability layer with no user-visible behavior change, admin user management (`UsersFlowTests` - Milestone 16.1) - admin-area authorization for `/Admin/Users` the same CanManageUsers shape (anonymous redirected to login, a `Customer` rejected with a real `403`, an `OrderManager` also rejected with a real `403` since the policy is SuperAdmin/Admin-only, an `Admin` able to view the list), a SuperAdmin creating a new staff account end-to-end and the new account being immediately usable to log in, a SuperAdmin changing an existing user's role end-to-end (confirmed via `UserManager.GetRolesAsync` directly), deactivating an account blocking its very next login attempt through the real `AuthService.ValidateCredentialsAsync` gate and reactivating it restoring login, an admin being unable to deactivate or change the role of their own account (both rejected server-side, confirmed via `UserManager` directly afterward), and unlocking a real Identity lockout (driven by five real failed login attempts) letting the account log in again, the audit log viewer (`AuditLogFlowTests` - Milestone 16.2) - admin-area authorization for `/Admin/AuditLog` the same CanManageUsers shape (anonymous redirected to login, a `Customer` rejected with a real `403`, an `OrderManager` also rejected with a real `403`), a real registration over the storefront's own register form producing a visible `RegisterSuccess` audit entry findable by searching the new account's email, and the CSV export endpoint returning a real `text/csv` response containing that same entry for the current search filter, store configuration (`SettingsFlowTests` - Milestone 16.3) - admin-area authorization for `/Admin/Settings` the same CanManageUsers shape as Users/AuditLog (anonymous redirected to login, a `Customer` rejected with a real `403`, an `OrderManager` also rejected with a real `403`, an `Admin` able to view the page), a SuperAdmin updating settings through the real form and the storefront homepage reflecting the new store name immediately on the very next request (proving the `IMemoryCache` invalidation actually works end-to-end, not just at the unit level), and updating with a stale `RowVersion` (captured from an earlier page load, after someone else's update has since advanced it) returning the "changed by someone else" conflict message rather than silently overwriting - deliberately exercised here against the real SQL Server test database rather than as a unit test, since EF Core's InMemory provider doesn't reliably reproduce SQL Server's rowversion concurrency-token behavior; every submitted form in this test class holds `DefaultTaxCountryCode`/`DefaultShippingCountryCode` at "PK" to avoid disturbing `EstimatedTaxFlowTests`/`EstimatedShippingFlowTests`, which share this same collection-wide database and rely on that default jurisdiction staying put, security response headers and CORS (`SecurityHardeningFlowTests` - Milestone 17.1) - a real HTTP response (both an MVC page and an `/api/*` route) carries `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, `Permissions-Policy`, and a `Content-Security-Policy` containing the expected `default-src`/`object-src`/`frame-ancestors` directives, a cross-origin request from an origin not on the configured allow-list gets no `Access-Control-Allow-Origin` header at all, and a request from an origin the test factory explicitly allow-lists (`Cors:AllowedOrigins` test override in `AuthWebApplicationFactory`) gets that exact origin echoed back, and complete admin-UI flows (`ProductAdminFlowTests`, `SupplierAuthorizationTests`). |

## Running tests

```
dotnet test
```

With coverage (Coverlet, Cobertura format under `TestResults/`):

```
dotnet test --collect:"XPlat Code Coverage" --results-directory ./TestResults
```

## Test database safety

- Automated tests must never run against `ECommerceAppDb` (development) or
  any production database. **`ECommerceAppTestDb`** (on the local LocalDB
  instance, `(localdb)\MSSQLLocalDB`) is used by
  `ECommerceApp.IntegrationTests`'s auth tests (`TestSupport/TestDatabase.cs`,
  `AuthWebApplicationFactory`), which apply migrations and wipe the
  auth-related tables once per test collection
  (`AuthTestFixture.InitializeAsync`) for a clean, deterministic slate.
- The Foundation-level smoke tests in `ApplicationStartupTests` originally
  didn't need a reachable database (a placeholder connection string was
  enough to satisfy DI registration). That stopped being true at Milestone
  4.1: the public layout's category nav and the home page itself now query
  real catalog data on every request, so this class was moved onto the same
  shared `AuthTestFixture`/`ECommerceAppTestDb` every other integration test
  class uses. See `Architecture.md`'s "Storefront home page composition"
  section for the full reasoning.
- `TestDatabase.ResetAsync`'s cleanup script must gain a `DELETE` (and
  identity reseed) for every new table a milestone introduces, the same
  session it's introduced - missing this for `Promotions`/`TaxRates` (M7.1/
  M7.2) went unnoticed since those tests happened to isolate themselves via
  a per-test-unique coupon code/tax category, but it caused a real,
  order-dependent test failure once `ShippingMethods` (M7.3) was added and
  had no equivalent per-test-randomizable dimension. See `Architecture.md`'s
  Milestone 7.3 section for the full account.
- From Milestone 18 onward, the full test-database bootstrap must also
  actively reject any connection string that looks like the dev or
  production database name.
- **A new table with a `Restrict` FK to `Products` (or any other table
  `TestDatabase.ResetAsync` deletes) must be added to that same cleanup
  script in the milestone that introduces it**, deleted before its parent.
  Missed once for `CartItems`/`Carts` (Milestone 6.1) - the very next
  integration test run after `CartFlowTests` left rows behind failed 64
  unrelated tests at cleanup time with a foreign key violation on `DELETE
  FROM Products`. See `Milestone-Status.md`'s Milestone 6.1 bugs section.

## Manual curl testing vs. automated HttpClient tests (Milestone 2)

Manually driving the running app with `curl` + shell pipelines produced two
false alarms this milestone that automated `HttpClient`-based tests
immediately disproved:

1. Extracting the antiforgery token with plain `grep` (which returns *all*
   matches) silently concatenated two tokens with a newline whenever a page
   rendered more than one antiforgery-protected form (e.g. the "new
   attribute" form plus one "add value" form per existing attribute) -
   always a 400. `HtmlHelpers.ExtractAntiForgeryToken` (C#, `Regex.Match`,
   first match only) doesn't have this problem. Fix: pipe through `head -1`
   before extracting the value in shell, or just trust an automated test
   over a curl chain when they disagree.
2. `HttpResponseMessage.RequestMessage.RequestUri` does not reliably reflect
   the final URL after auto-redirect through `WebApplicationFactory`'s
   in-process `TestServer` - read the `Location` header directly off the
   redirect response instead (with `AllowAutoRedirect = false`) when a test
   needs to recover a newly-created entity's id.

Neither was an application bug. The moral isn't "don't test manually" - it's
that a manual curl session and an automated `HttpClient` test can disagree,
and when they do, the automated test (which doesn't accumulate shell/grep
footguns) is the one to trust; write it before spending more time on the
curl side.

## A real bug this milestone's tests caught

Running the full `ECommerceApp.IntegrationTests` suite together (not just one
test at a time) surfaced two real issues that a narrower test run would have
missed:

1. The rate limiter was **unpartitioned** (one global counter for all
   clients), so the test suite's own traffic exhausted the login/register
   quota within seconds - the same bug would let one abusive client in
   production lock out every other user. Fixed by partitioning per client IP.
2. Several `IConfiguration` reads (JWT bearer options, the rate limiter's
   permit limit, originally the DB connection string) were captured **before**
   `WebApplicationBuilder.Build()`, so they never saw `WebApplicationFactory`
   test overrides (which only merge in at `Build()` time) - see
   `Architecture.md`'s "Configuration resolution timing" section.

Moral: a feature that only gets exercised by one isolated test, or only ever
tested via `WebApplicationFactory` with its defaults, can hide bugs that only
show up under the conditions the feature exists for (concurrent real users,
or genuinely varying configuration). Prefer running the whole suite, not
just the new tests, before calling a milestone done.

## EF Core InMemory provider does not support transactions (Milestone 3.1)

`InventoryService` opens an explicit `Database.BeginTransactionAsync()` for
every multi-write operation (recording opening stock, adjusting stock,
reserving/releasing stock), per the brief's "use transactions ... throughout"
requirement for inventory. The EF Core InMemory provider used by
`Infrastructure.Tests` throws `InvalidOperationException` the instant one is
requested - not documented anywhere obvious, only discovered by running the
new tests and reading the stack trace. A misleadingly "passing" one-off probe
test earlier in development returned success without exercising this path at
all (it hit an early-exit validation branch before reaching the transaction
call), which is itself a small lesson: a probe test that doesn't fail is not
the same as a probe test that proves the code path was exercised. Fixed by
gating transaction creation on `_dbContext.Database.IsRelational()` - see
`Database-Design.md`'s "Explicit transactions and the EF Core InMemory
provider" section. Any future service that opens an explicit transaction and
is unit-tested against the InMemory provider will need the same guard.

## Test-process parallelism

`ECommerceApp.IntegrationTests` sets
`[assembly: CollectionBehavior(DisableTestParallelization = true)]`
(`AssemblyInfo.cs`) because every `WebApplicationFactory<Program>` instance
boots the same `Program.cs`, which uses Serilog's shared static `Log.Logger`
and closes it in a `finally` block - concurrent factories (xUnit's default
across collections) could have one factory's shutdown tear down the logger
mid-startup for another. This was an actual observed flake, not a
precaution.

## Architecture tests

`ArchitectureTests.cs` inspects each compiled assembly's real references
(`Assembly.GetReferencedAssemblies()`), not just `.csproj` `ProjectReference`
entries, and fails if a dependency-rule violation is introduced (e.g. if
`Domain` ever starts referencing EF Core, or `Application` starts
referencing `Infrastructure`).

## Coverage targets

Per-milestone coverage isn't gated numerically until Milestone 18, which sets
the project-wide targets (>=80% Application-layer business logic, >=70%
overall meaningful line coverage). Earlier milestones should still cover
every new business rule; padding with meaningless tests is explicitly out of
scope.
