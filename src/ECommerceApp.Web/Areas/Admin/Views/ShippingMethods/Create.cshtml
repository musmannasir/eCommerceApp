@model ShippingMethodFormViewModel
@{
    ViewData["Title"] = "New shipping method";
}

<h1 class="h3 mb-4">New shipping method</h1>

<form asp-action="Create" method="post">
    @await Html.PartialAsync("_Form", Model)
</form>
